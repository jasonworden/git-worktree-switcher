#!/usr/bin/env bash
# Run from any directory: resolves repo root from this script's location.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")/.." && pwd)"
exec bash "$ROOT/scripts/wt-rebuild.sh" --eval "$@"
